// Base colors CSS classes constants
let Colors = {
    OFF: "btn-outline-secondary",
    ON: "btn-outline-danger",
};

// Codes to CSS classes relationship
Colors.codes = {
    "off": Colors.OFF,
    "on": Colors.ON
};

// Code to name color relationship
Colors.names = {
    "off": "Off",
    "on": "On"
};

Colors.htmlCodes = {
    "off": "#888888",
    "on": "#fb4541",
};

Colors.codesToCSSClass = {
    "#888888": Colors.OFF,
    "#fb4541": Colors.ON
};

export default Colors;